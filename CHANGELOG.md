# `0.0.4`

- Changed rules:
  - `no-shadow` ⇒ `off`;
  - `require-await` ⇒ `off`;
  - `radix` ⇒ `['error', 'as-needed']`.
- Removed rules:
  - `no-multi-assign`.

# `0.0.3`

- Changed rules:
  - `sort-imports` ⇒ `off`.
